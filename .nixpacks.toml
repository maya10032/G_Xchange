[phases.setup]
command = "nix-env -iA nixpkgs.php82"  # PHP 8.1を指定

[phases.build]
command = "composer install --no-dev --optimize-autoloader"

[phases.start]
command = "php artisan serve --host=0.0.0.0 --port=${PORT:-8000}"
